/* 
Stack is a data structure that follows LIFO(LAst In First Out)
techniques which means data is inserted at the top and also removed
from the top.

This a abstract & higher level data structure.We learned array , linked list , actually
they are lower level data structure

Stack operation:
-push()
-pop()
-top()
-size()
-empty()
*/